import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#7266ab",
    },
    secondary: {
      main: "#5f3c81",
    },
    background: {
      default: "#eceaf6",
    },
  },
  typography: {
    fontSize: 16,
  },
});
export default theme;
